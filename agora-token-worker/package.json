{
  "name": "agora-token-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for generating Agora RTC tokens",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "setup:secrets": "echo 'Setting up secrets...' && wrangler secret put AGORA_APP_ID && wrangler secret put AGORA_APP_CERTIFICATE",
    "logs": "wrangler tail --format pretty",
    "logs:prod": "wrangler tail --format pretty --env production",
    "test": "node scripts/test-api.js"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.22.4"
  }
}
